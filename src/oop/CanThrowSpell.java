package oop;

import java.util.List;

public interface CanThrowSpell {
    //tylko metody abstrakcyjne (public i abstract)
    //pola -> finalnymi i statycznymi i publiczne
    //metody default
    //interfejs moze wystepowac jako typ (jako obiekt)
    void throwSpell();
}
